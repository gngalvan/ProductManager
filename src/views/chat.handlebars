<!DOCTYPE html>
<html lang="en">

<!DOCTYPE html>
<html>

<head>
    <title>Chat</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .chat-container {
            width: 400px;
            height: 400px;
            border: 1px solid #ccc;
            overflow-y: scroll;
        }

        .chat-message {
            padding: 5px;
            margin-bottom: 10px;
            background-color: #f2f2f2;
        }

        .chat-message .sender {
            font-weight: bold;
        }

        .chat-input {
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="chat-container" id="chatContainer">
            {{#each messages}}
            <div class="chat-message">
                <span class="sender">{{sender}}</span>: {{message}}
            </div>
            {{/each}}
        </div>

        <div class="input-group mt-3">
            <input type="text" class="form-control chat-input" id="messageInput" placeholder="Escribe tu mensaje...">
            <div class="input-group-append">
                <button class="btn btn-primary" id="sendButton">Enviar</button>
            </div>
        </div>
    </div>

    <script src="/js/chat.js"></script>
</body>

</html>